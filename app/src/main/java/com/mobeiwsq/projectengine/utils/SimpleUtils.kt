package com.mobeiwsq.projectengine.utils

import android.content.Context
import android.util.DisplayMetrics

fun getDisplayMetrics(context: Context): DisplayMetrics {
    return context.resources.displayMetrics
}